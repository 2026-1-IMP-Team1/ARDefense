public enum TurretType
{
    // Specifies the types of turrets that can be classified according to future turret designs.
    // Normal turret, slow attack turret, etc. ...
    // (04/16) Changed to one turret per era + upgrade to increase stats
    // Defines one type of turret for each era to allow different assets to be applied for each era.
    MIDDLE_AGE_TURRET,
    MODERN_AGE_TURRET,  
    FUTURE_AGE_TURRET
}
